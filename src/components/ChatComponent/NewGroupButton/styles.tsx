import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flexDirection: "row", 
        padding: 10, 
        alignItems: "center"
    },
    text: {
        marginLeft: 10, 
        fontWeight: "bold"
    }
});

export default styles;
