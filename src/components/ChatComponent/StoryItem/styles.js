import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    imageContainer: {
       padding: 10,
    },

    image: {
        width: 55,
        height: 55,
        borderRadius: 30,
    },
    name: {
        fontWeight: "bold",
        fontSize: 17,
    },
     
})

export default styles