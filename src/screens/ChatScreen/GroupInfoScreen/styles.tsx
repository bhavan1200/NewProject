import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    root: {
        backgroundColor: "#fff",
        padding: 10,
        flex: 1
    },
    title: {
        fontSize: 18,
        fontWeight: "bold",
        paddingVertical: 10
    }
});

export default styles;
